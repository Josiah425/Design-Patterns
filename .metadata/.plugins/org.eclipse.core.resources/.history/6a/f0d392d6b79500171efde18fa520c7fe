import java.io.File;

public class MyArrayListTest {
	private MyArrayList list = new MyArrayList();
	
	public boolean insertUniquesTest(File fin){
		try{
			FileProcessor fProcessor = new FileProcessor(fin);
			String line = fProcessor.readLine();
			while(line != null){
				int integer = Integer.parseInt(line);
				list.insertSorted(integer);
				line = fProcessor.readLine();
				list.toStr();
			}
			fProcessor.close();
		}
		catch(Exception e){
			e.printStackTrace();
		}
		return true;
	}
	
	public boolean removeUniquesTest(File fin){
		try{
			FileProcessor fProcessor = new FileProcessor(fin);
			String line = fProcessor.readLine();
			while(line != null){
				int integer = Integer.parseInt(line);
				list.removeValue(integer);
				line = fProcessor.readLine();
				list.toStr();
			}
			fProcessor.close();
		}
		catch(Exception e){
			e.printStackTrace();
		}
		return true;
	}
	
	public boolean insertDuplicate(File fin){
		try{
			FileProcessor fProcessor = new FileProcessor(fin);
			String line = fProcessor.readLine();
			while(line != null){
				int integer = Integer.parseInt(line);
				
			}
		}
		catch(Exception e){
			e.printStackTrace();
		}
		return true;
	}
	
	public boolean removeDuplicate(File fin){
		
		return true;
	}
	
	public boolean insertOverFifty(File fin){
		
		return true;
	}
	
	public boolean removeOverFifty(File fin){
		
		return true;
	}
	
	public boolean removeEmpty(File fin){
		
		return true;
	}
	
	public boolean insertOutOfRange(File fin){
		
		return true;
	}
	
	public boolean indexOfNotFound(File fin){
		
		return true;
	}
	
	public boolean sumOfList(File fin){
		
		return true;
	}
	
	public boolean sizeOfList(File fin){
		
		return true;
	}
}
